# 反思报告

TODO(全员，C 汇总)：撰写反思报告，作为课程关键交付物。需包含：

1. Agent 核心循环（ReAct）的逐行解读 —— 见 `Core/Agent/ReActAgent.cs`。
2. 关键设计决策与取舍：为何手写循环（而非高层黑盒）、两层编排为何顶层动态/底层固定、
   角色按判断维度分而工具按能力分、配置为何用 appsettings.Local.json 分层。
3. 工具 vs Agent vs MCP 的概念区分与本项目中的体现。
4. AI 辅助开发的使用情况：如实记录哪些代码由 AI 生成、哪些自行编写。
5. 遇到的问题与解决、可改进之处。
